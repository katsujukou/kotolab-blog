{
  "type": "module",
  "scripts": {
    "compile": "cd .. && spago build -p web",
    "optimize": "cd .. && purs-backend-es build",
    "pack": "vite build",
    "build": "npm run compile && npm run optimize && npm run pack",
    "dev": "pnpm run compile && vite"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.12",
    "vite": "^5.4.6"
  }
}